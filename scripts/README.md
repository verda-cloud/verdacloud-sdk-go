# Scripts

General project scripts. Test-related helpers (availability check, integration test assist) live under **[test/scripts/](../test/scripts/)**.

## release.sh

Updates `CHANGELOG.md` with a new version section. Run from repository root:

```bash
./scripts/release.sh <version>
# Example: ./scripts/release.sh v1.0.0
```
